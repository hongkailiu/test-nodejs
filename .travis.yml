language: node_js

node_js:
  - "node"
  - "lts/*"
  - "10"

before_script:
  - npm --version
  - npm install -g @angular/cli
  - ng --version
  - npm install -C ./home-ng/

script:
  - make -C ./home-ng/ test build
  - make -C ./home-ng/ pre-release
