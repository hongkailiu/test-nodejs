language: node_js

node_js:
  - "node"
  - "lts/*"
  - "10"

install:
  - npm --version
  - npm install -g @angular/cli
  - ng --version

script:
  - make -C ./home-ng/ test build
  - make -C ./home-ng/ pre-release
